# 模型假设、符号与模型表述

## 模型假设

只保留建模所必需、能够简化模型且具有合理依据的假设。对每项重要假设，记录其必要性、支持依据、受影响的方程或约束、假设不成立时可能造成的偏差，以及可执行的检验或灵敏度分析。删除常识性陈述，以及未被方程或验证使用的假设。若目前无法检验，应将该假设标为“未验证”，并限定受其影响的结论范围。

部分假设可以由一次试验或预实验的观察提出，但必须给出假设理由：说明试验条件、观察到的现象或结果，以及其如何支持该假设。单次试验不能被表述为普遍规律；应同时说明其可验证范围和可能的偶然性。

## 符号说明

- 一个符号只表示一个含义，并在首次出现时定义。
- 标量、向量、矩阵、下标、随机变量和估计量的记号约定应保持一致。
- 注明单位和下标取值范围。
- 不要直接把程序变量名当作数学符号。

## 模型建立

按照“现实问题 -> 数学定义 -> 推导过程 -> 模型方程 -> 变量含义”的顺序表述。方程之间必须有承接推理，不能只堆叠公式而不解释各式为何成立。

先写明模型的研究对象、边界和实验目标，再叙述从已知条件、作用机制到待解释结果的前因后果。说明为何选择该模型，以及它与研究对象、实验目标和可获得数据之间的对应关系。对于物理问题，应给出现实机理，并说明针对该实验目标的常规模型选择；如存在优化模型或替代模型，应说明其改进点、适用条件和本题是否采用的理由。

## 求解过程

说明求解器或算法、参数、初始条件、软件、随机种子（如适用）、容差、停止条件和重复运行方案。用足以复现结果的细节替代“用 Python 求解得到”。缺少代码、数据或运行环境时，不得声称结果可以复现。

明确求解对应的实验对象、实验目标和输出指标，并说明选择该求解算法的理由，例如其如何匹配模型结构、约束条件、精度要求或数据规模。可以查阅参考文献以支持算法选择；若要在论文中使用或引用文献，必须先请求权限审批，未获批准不得将其作为论据或引用。 
