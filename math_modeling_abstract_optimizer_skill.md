---
name: math-modeling-abstract-optimizer
description: >
  面向数学建模竞赛论文的摘要与题目优化 Skill。用于在不虚构模型、数据、结果或结论的前提下，
  从论文终稿、建模过程、求解结果与题目要求中提取证据，优化论文题目、中文/英文摘要与关键词，
  强化“问题—方法—结果—结论”的闭环、分问覆盖、量化表达、术语一致性和竞赛论文可读性。
version: 1.0
language: zh-CN
tags:
  - 数学建模
  - 论文摘要
  - 标题优化
  - 关键词
  - 学术写作
  - 结果量化
---

# 数学建模论文摘要优化 Skill

## 1. Skill 目标

将数学建模论文的摘要从“内容罗列”优化为“高密度的建模成果展示”。

最终摘要应让评阅者快速回答以下问题：

1. **研究了什么问题？**
2. **每个子问题分别用了什么模型或方法？**
3. **为什么这些方法适合该问题？**
4. **得到了哪些关键、可核验、量化的结果？**
5. **模型的创新、改进或价值是什么？**
6. **全文的题目、摘要、关键词和正文术语是否一致？**

本 Skill 同时负责：
- 论文题目优化；
- 中文摘要优化；
- 按“针对问题 X”组织的分问版摘要；
- 英文摘要的结构与术语一致性优化；
- 关键词提炼；
- 摘要质量自检。

---

## 2. 适用场景

在以下场景调用本 Skill：

- 数学建模竞赛论文已经完成主体建模，希望优化摘要；
- 已有摘要，但存在逻辑松散、方法堆砌、结果不量化等问题；
- 需要把多个子问题压缩成高度概括的摘要；
- 需要统一题目、摘要、正文中的模型名称；
- 需要生成“普通摘要版”和“针对问题 X”分问版；
- 需要将中文摘要转换为结构一致的英文摘要；
- 需要从全文提炼关键词。

不建议在以下情况下直接调用：
- 论文尚无明确模型与求解结果；
- 关键结论尚未计算完成；
- 用户仅需要润色单个句子；
- 用户希望生成不存在于论文中的“更漂亮结果”。

---

## 3. 输入要求

### 3.1 最佳输入

尽可能提供以下材料：

1. 赛题原文或题目要求；
2. 论文终稿或接近终稿的版本；
3. 现有论文题目；
4. 现有摘要；
5. 每个子问题的：
   - 问题目标；
   - 核心模型；
   - 求解算法；
   - 参数估计方式；
   - 关键结果；
   - 误差、精度、评价指标；
   - 灵敏度/鲁棒性分析结果；
6. 论文中的创新点或模型改进；
7. 比赛官方对摘要页数、字数、格式的限制。

### 3.2 信息不足时

若输入缺少关键结果：

- **不得自行编造数值、误差、精度、概率、提升比例或实验结论；**
- 可保留 `[待补充：关键数值]`、`[待补充：误差指标]` 等占位符；
- 明确指出哪些内容缺少证据；
- 优先输出“可安全优化的版本”，而不是为了完整而虚构。

---

## 4. 最高优先级规则

### Rule 1：事实忠实优先于语言华丽

所有模型、算法、数据、指标、结论必须来自用户提供的论文、代码输出、表格、图表或明确说明。

禁止：
- 为增强说服力虚构数字；
- 擅自提高精度；
- 将“趋势”改写为“显著提升”；
- 将“相关关系”写成“因果关系”；
- 将“局部验证”写成“普遍有效”；
- 将没有进行过的敏感性、鲁棒性或误差分析写入摘要。

### Rule 2：量化优先，但不强行量化

有数据时优先写：
- 最优值；
- 预测值；
- 误差；
- 准确率；
- RMSE / MAE / MAPE / $R^2$；
- 成本下降比例；
- 收益提升比例；
- 利用率；
- 概率；
- 置信区间；
- 灵敏度分析范围；
- 鲁棒性结果。

没有可靠数字时，不得制造数字。可以使用：
- “结果表明……”
- “模型识别出……”
- “最优方案为……”
- “敏感性分析显示……”
但必须与论文中的真实结论一致。

### Rule 3：摘要必须形成闭环

每个核心子问题尽量形成：

> **问题目标 → 建模方法 → 求解方式 → 核心结果 → 解释/价值**

禁止只写“建立了模型 A、模型 B、模型 C”，却不说明各模型解决了什么、得到什么结果。

### Rule 4：结果的重要性高于背景篇幅

摘要空间不足时，信息保留优先级为：

1. 核心结果；
2. 问题与模型的对应关系；
3. 方法或算法创新；
4. 结论与应用价值；
5. 背景介绍。

背景不能挤占结果展示空间。

### Rule 5：术语全篇一致

自动检查：
- 模型中文名是否统一；
- 英文名和缩写是否统一；
- NSGA-II、ARIMA、LSTM、XGBoost 等名称是否规范；
- “贝叶斯反演 / Bayesian inversion”等中英文是否对应；
- 同一变量、同一指标不能在摘要中更换叫法；
- 标题中的模型名称应与正文实际核心模型一致。

---

## 5. 工作流程

## Step 0：建立“摘要证据清单”

在正式改写前，先从论文中提炼以下信息：

| 子问题 | 核心目标 | 核心模型/方法 | 求解/算法 | 关键结果 | 误差/评价 | 创新或价值 |
|---|---|---|---|---|---|---|
| 问题 1 |  |  |  |  |  |  |
| 问题 2 |  |  |  |  |  |  |
| 问题 3 |  |  |  |  |  |  |
| 问题 4 |  |  |  |  |  |  |

若某一格没有论文证据，标记为“缺失”，不得补造。

### 证据筛选原则

每个子问题优先选：
- 1 个最核心模型；
- 1 个最关键算法或改进；
- 1～3 个最有代表性的结果；
- 1 个能说明可靠性的评价指标；
- 1 个最有价值的结论。

不要把正文所有模型全部塞入摘要。

---

## Step 1：诊断现有摘要

检查是否存在以下常见问题：

### 内容问题
- 背景过长；
- 没有明确写出每个子问题；
- 方法多、结果少；
- 结果只写“效果良好”“具有较高精度”；
- 创新点只有口号，没有对应模型改进；
- 摘要出现正文中不存在的方法；
- 摘要与最终代码结果不一致。

### 逻辑问题
- 问题与方法错配；
- 方法与结果之间缺少逻辑连接；
- 多个子问题揉成一段，无法区分；
- 结果没有说明对应哪个问题；
- 结论重复前文，不提供新信息。

### 表达问题
- “首先、其次、然后、最后”过多；
- 大量空泛修饰词；
- 连续堆叠专业术语；
- 句子过长；
- 过度使用“我们”；
- 重复出现“建立模型”“进行分析”等低信息密度表达。

---

## Step 2：优化论文题目

### 2.1 标题目标

题目应同时体现：

- **研究对象 / 核心问题**
- **核心模型 / 方法**
- 必要时体现**优化、预测、评价、反演、调度、分配、决策**等任务属性

### 2.2 推荐结构

优先考虑：

> **基于 XXX 模型/方法的 XXX 问题研究**

或：

> **基于 XXX 与 XXX 的 XXX 优化研究**

或：

> **XXX 问题的 XXX 建模与优化**

源提示词强调“基于 XXX 模型/方法的 XXX 问题优化研究”结构。实际使用时，不必机械套用；若题目并非优化类任务，应根据真实任务改用“预测、评价、识别、反演、分析、决策”等准确动词。

### 2.3 标题约束

- 建议 20～30 个汉字左右；
- 避免“关于……的研究”等弱信息表达；
- 不在标题中塞入过多算法名；
- 标题中的模型必须确实是论文核心模型；
- 不用“创新型”“高精度”“智能化”等无证据修饰；
- 题目、摘要、正文中的模型名称保持一致。

---

## Step 3：优化摘要主结构

摘要使用以下逻辑结构：

> **背景 → 问题 → 方法 → 结果 → 结论**

这里的“五段式”首先是**逻辑结构**，不一定必须排成五个视觉段落。若竞赛要求摘要为一个整体段落，也应在一个段落中完成上述逻辑闭环。

---

### 3.1 背景：说明为什么需要建模

建议占全文约 10%～15%。

写作目标：
- 交代题目场景；
- 点出实际痛点；
- 说明建模必要性；
- 快速进入问题。

推荐写法：

> 针对【研究场景】中存在的【核心困难/决策需求】，本文围绕【总目标】建立数学模型，以解决【核心问题概括】。

避免：
- 大段行业介绍；
- 宏观政策口号；
- 与模型无关的社会意义；
- “随着社会不断发展……”等模板句。

---

### 3.2 问题：准确覆盖各小问

对每个子问题，明确其任务类型，例如：

- 预测；
- 分类；
- 评价；
- 优化；
- 反演；
- 调度；
- 路径规划；
- 资源分配；
- 参数估计；
- 多目标决策；
- 风险分析。

优先把子问题压缩成一句高密度任务描述，而不是照抄赛题原文。

---

### 3.3 方法：强调“为什么这样建模”

方法部分不能只是模型名单。

推荐结构：

> 针对【问题】，考虑到【数据/机理特征】，构建【核心模型】，并采用【算法/估计方法】进行求解；进一步通过【改进方法】处理【原模型局限】。

对于复杂模型链，应写清关系：

> 数据预处理 → 参数估计 → 主模型 → 优化算法 → 验证

而不是：

> 使用 A、B、C、D、E 五种方法进行建模。

### 方法写作优先级

1. 主模型；
2. 关键求解算法；
3. 真正影响结果的改进；
4. 必要的数据处理；
5. 次要辅助方法。

---

### 3.4 结果：摘要的核心

结果部分应占较高信息密度。

每个子问题优先写：

> **核心数值 + 指标含义 + 对应结论**

例如：

> 模型得到方案 A 的资源利用率为 92.3%，相较基准方案提高 8.1%，同时总成本下降 6.4%。

比以下写法更强：

> 模型取得了良好效果，并有效降低成本。

### 结果选择规则

优先选择具有以下特征的结果：
- 直接回答赛题问题；
- 可以量化；
- 能比较不同方案；
- 能体现模型可靠性；
- 能体现实际决策价值；
- 能体现模型改进的收益。

### 数值表达规则

- 不使用没有依据的过度精确小数位；
- 百分比提升必须有明确基准；
- “误差 5%”应尽量说明是什么误差；
- “准确率 98%”应说明评价任务和数据条件；
- 若结果来自模拟，应使用“模拟结果表明”；
- 若结果来自预测，应使用“预测结果显示”；
- 若存在置信区间、灵敏度或鲁棒性结果，优先选择能增强可信度的指标。

---

### 3.5 结论：提炼创新与应用价值

结论不能只是重复结果。

建议回答：
- 模型解决了什么核心困难？
- 与直接/传统方法相比，改进在哪里？
- 结果可支持什么实际决策？
- 模型可以推广到什么**相似条件**下？

推荐结构：

> 结果表明，所建模型能够【核心能力】，其中【主要创新/改进】提升了【具体方面】。该框架可为【实际对象】提供【决策/预测/评价】依据，并可推广至具有【相似特征】的相关问题。

避免：
- “具有重要现实意义”；
- “具有广阔应用前景”；
- “可推广到所有类似问题”；
- 无证据的夸张性结论。

---

## Step 4：生成“针对问题 X”分问版摘要

当论文包含多个明确小问时，优先提供一个分问清晰版本。

模板：

> 【背景引入】  
> 针对问题 1，……建立【模型】……采用【算法】……得到【关键结果】……  
> 针对问题 2，……  
> 针对问题 3，……  
> 最后，……【总结模型创新、稳定性或应用价值】。

### 每个分问必须尽量包含

1. **具体任务**
2. **核心模型**
3. **求解方法**
4. **关键结果**
5. **必要时给出评价/验证**

### 分问版的压缩规则

若字数超限：
1. 删除重复背景；
2. 删除次要模型名；
3. 合并重复的数据预处理；
4. 保留每问最关键的 1～2 个结果；
5. 保留能体现模型可靠性的 1 个指标；
6. 将共同的验证方法放到结尾统一说明。

---

## Step 5：关键词提炼

选择 3～5 个关键词。

关键词应覆盖：

- 1～2 个核心模型或方法词；
- 1～2 个研究主题词；
- 必要时 1 个关键任务词。

示例结构：

> Bayesian 反演；Archard 磨损模型；参数估计；楼梯磨损分析

### 关键词规则

- 每个关键词尽量简洁；
- 不选“数学建模”“数据分析”等过泛词；
- 不选摘要中未出现的模型；
- 不堆叠高度相似词；
- 与正文高频核心术语一致；
- 中文关键词通常使用分号分隔。

---

## Step 6：英文摘要优化

仅在中文摘要或论文证据稳定后生成英文版。

### 英文摘要规则

- 不直译中文句式；
- 保留全部真实数值；
- 模型名、算法名、指标名保持标准写法；
- 首次出现的缩写给出全称（若有必要）；
- 不为了“学术感”添加中文原文没有的结论；
- 避免重复使用：
  - “In order to...”
  - “This paper mainly...”
  - “It can be seen that...”
- 优先使用客观、简洁、信息密度高的表达。

若比赛无其他规定，可参考：
- 中文摘要：约 800～1000 字以内；
- 英文摘要：约 300～500 words；
- 但**官方模板或当届竞赛规定永远优先于本 Skill 的默认长度建议**。

---

## 6. 推荐的摘要信息配比

以下为默认参考，不是硬性规定：

| 模块 | 建议占比 |
|---|---:|
| 背景与总体目标 | 10%～15% |
| 问题概括 | 10%～15% |
| 方法与模型 | 25%～30% |
| 核心结果 | 35%～40% |
| 结论与价值 | 10%～15% |

若篇幅不足，优先保留“方法—结果”的闭环。

---

## 7. 模型创新点的安全表达

只有正文中确实存在以下内容时，才能写“创新”：

- 对经典模型做结构改进；
- 引入新的约束；
- 组合不同模型形成合理框架；
- 修改目标函数；
- 改进参数估计方式；
- 设计新的评价指标；
- 引入动态机制；
- 融合机理模型与数据驱动模型；
- 提升求解效率；
- 增加鲁棒性或不确定性处理。

### 推荐表达

- “在 XXX 模型基础上引入……”
- “针对 XXX 假设过强的问题，加入……约束”
- “将 XXX 与 XXX 融合，以同时刻画……”
- “通过 XXX 校准参数，降低……带来的偏差”
- “采用 XXX 替代固定参数，使模型能够反映……”

### 避免表达

- “首次提出”
- “开创性地”
- “国际领先”
- “显著优于所有方法”
- “完全解决”

除非用户提供充分证据。

---

## 8. 常见低质量表达替换规则

| 弱表达 | 优先替换方向 |
|---|---|
| 效果良好 | 给出准确率、误差、目标函数值或比较结果 |
| 模型合理 | 给出验证、残差、拟合优度或敏感性结果 |
| 具有意义 | 指明对哪一类决策有何价值 |
| 建立了多个模型 | 说明每个模型分别解决什么问题 |
| 进行了预测 | 给出预测对象、预测值或评价指标 |
| 得到了最优方案 | 给出方案关键决策量和目标值 |
| 模型稳定 | 给出参数扰动下结果变化 |
| 方法有效 | 指明对比基线和改进幅度 |
| 使用机器学习模型 | 写明具体模型及其任务 |
| 进行了数据处理 | 只保留真正影响模型的关键处理 |

---

## 9. 摘要压缩策略

当摘要超过限制时，依次执行：

### 第一轮：删低价值背景
删除：
- 常识性背景；
- 政策性套话；
- 题目原文重复；
- 与建模无直接关系的意义说明。

### 第二轮：合并方法
将多个辅助方法合并为：

> “经数据预处理与参数标定后……”

而不是逐一罗列所有工具。

### 第三轮：筛选结果
每个子问题只保留：
- 最终答案；
- 最关键的 1～2 个指标；
- 能体现模型可靠性的结果。

### 第四轮：删除重复结论
若结果已经体现“成本下降”，结尾不再重复“降低了成本”，而应说明模型的决策意义或可推广条件。

---

## 10. 输出格式

默认按以下顺序输出。

### A. 优化后论文题目

给出 1 个推荐版本。

必要时可补充 2 个备选题目，但不得为了“显得高级”加入正文没有的模型。

### B. 优化后摘要（推荐版）

输出可直接用于论文的完整摘要。

要求：
- 逻辑完整；
- 不插入解释性批注；
- 不使用虚构数据；
- 模型与结果一一对应；
- 语言凝练。

### C. 分问版摘要

当论文包含多个子问题时输出：

- 针对问题 1……
- 针对问题 2……
- 针对问题 3……

若用户明确只需要单段版，可不输出此部分。

### D. 关键词

格式：

> 关键词：词 1；词 2；词 3；词 4；词 5

### E. 信息缺口提示

仅在存在关键缺失信息时输出，例如：

- 问题 2 缺少最终预测值；
- 问题 3 只写“误差较小”，没有具体误差指标；
- 正文中模型名称存在两种写法；
- 摘要中的“提升 12%”未找到对应基准。

不要为了凑格式输出无意义的“无缺失”。

---

## 11. 最终自检清单

输出前逐项检查。

### 11.1 事实一致性

- [ ] 所有数字均能在论文/结果中找到来源
- [ ] 所有模型均在正文真实使用
- [ ] 没有添加未完成的实验
- [ ] 没有夸大因果关系
- [ ] 没有夸大模型适用范围

### 11.2 分问覆盖

- [ ] 每个核心问题均在摘要中出现
- [ ] 每个问题都有对应方法
- [ ] 每个问题至少有一个结果或结论
- [ ] 没有把问题 1 的结果写到问题 2

### 11.3 方法质量

- [ ] 明确主模型，而非只列算法名
- [ ] 模型选择与问题特征相匹配
- [ ] 真正的改进点被准确表达
- [ ] 辅助方法没有抢占主模型篇幅

### 11.4 结果质量

- [ ] 优先使用量化结果
- [ ] 百分比提升存在明确基线
- [ ] 误差指标含义清晰
- [ ] 数值精度合理
- [ ] 关键结果可以直接回答赛题问题

### 11.5 语言质量

- [ ] 无大段背景
- [ ] 无“效果良好”等空泛表达
- [ ] 无连续方法堆砌
- [ ] 句子长度适中
- [ ] 逻辑衔接自然
- [ ] 专业术语前后一致

### 11.6 题目与关键词

- [ ] 标题准确体现研究对象
- [ ] 标题中的核心方法确实重要
- [ ] 关键词包含核心模型和主题词
- [ ] 关键词与摘要、正文一致

---

## 12. 摘要质量评分 Rubric

可在内部按 100 分自评：

| 维度 | 分值 | 核心标准 |
|---|---:|---|
| 事实忠实性 | 25 | 不虚构模型、数字、结果、创新 |
| 分问覆盖与逻辑闭环 | 20 | 问题—方法—结果清晰对应 |
| 方法表达 | 15 | 主模型突出、适配性清楚、无堆砌 |
| 结果表达 | 20 | 量化、关键、能直接回答赛题 |
| 创新与价值 | 10 | 克制、具体、与正文一致 |
| 语言与格式 | 10 | 凝练、术语统一、符合篇幅要求 |

### 评分解释

- **90～100**：可直接进入论文排版，仅需人工核对数字；
- **80～89**：结构较好，但仍有少量结果或术语需要强化；
- **70～79**：信息基本齐全，但方法/结果闭环不足；
- **60～69**：摘要偏叙述，量化与分问对应不足；
- **<60**：建议重新基于论文证据构建摘要，而非继续局部润色。

---

## 13. 推荐执行指令

当用户上传数学建模论文后，可按以下方式执行：

> 阅读我提供的数学建模论文，先建立“子问题—模型—算法—结果—评价指标—创新点”的证据清单，再按照本 Skill 优化论文题目、摘要和关键词。任何数值、结论、模型或创新都必须来自论文证据，不得补造。摘要优先强化每个子问题的“问题—方法—结果”闭环，并给出普通版与“针对问题 X”分问版。若信息缺失，用明确的待补充标记指出，不要猜测。

---

## 14. 处理原则总结

本 Skill 的核心不是“把摘要写得更漂亮”，而是：

> **用最少的篇幅，准确展示最重要的建模工作与最可信的求解结果。**

优先级始终为：

> **真实性 > 闭环性 > 结果密度 > 模型创新 > 语言华丽度**

优秀的数学建模摘要应做到：

> **评阅者只读摘要，也能迅速理解论文解决了哪些问题、用了什么模型、得到了什么结果，以及这些结果为什么值得相信。**
