# Innovation-pattern refinement pressure-test result

## Scenario H4

Fresh evaluators received the crop-allocation deadline prompt and loaded only `cumcm-modeling` plus references/templates explicitly routed by it. They did not inspect the supplied documents, tests, reports or git history.

## RED baseline

The existing Skill already kept the exact MILP baseline, rejected decorative GNN/GA choices, proposed heterogeneous-yield and joint-scenario improvements, designed controlled ablations, bounded claims, and specified fallback rules. Candidate-screening dimensions were present only implicitly, and the response distinguished only pre/post-experiment wording rather than a reusable evidence ladder. Score: **4 pass, 2 partial**.

## GREEN and refactor observations

Early GREEN passes correctly assigned evidence level 0, added the 0–4 wording ladder and produced falsifiable records, but repeatedly compressed the seven screening dimensions. The final routing therefore requires the innovation portfolio to load the model-comparison template and preserve every column.

The final fresh evaluator returned:

- five candidates spanning structural, chain, objective/risk and solver choices;
- an explicit matrix containing baseline defect/evidence, problem value, new information, data support, decisive test, contest-budget risk, interpretability, paper clarity, risk tier, verdict and fallback;
- immediate rejection of GNN/GA because graph/data and measured solver-bottleneck prerequisites were absent;
- separate falsifiable records for the selected heterogeneous-yield and uncertainty extensions;
- controlled comparisons, artifact/constraint checks, pre-registration, applicability boundaries and discard actions;
- a level 0–4 evidence table that limited current wording to “propose/design” and separated significance, practical value, robustness and generality.

Verdict: **6/6 pass**. No external case count, unsupported experiment result or fashionable-algorithm exception was imported.
