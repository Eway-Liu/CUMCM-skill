# Paper-writing refinement pressure-test result

## Scenario H3

A fresh evaluator received the scheduling comparison prompt and loaded only `cumcm-thesis` plus references/templates routed by it. It did not inspect the two supplied reference documents, tests, reports or git history.

## RED baseline

The existing Skill already preserved source units, computed the 4% reduction correctly, rejected unsupported “significant” and “universally applicable” claims, exposed the 7/12 window instability and worse tail result, and proposed correlation testing before rebuilding scenarios. It did not consistently record a reversal condition, and its assumption review omitted explicit necessity and supporting-basis fields. Score: **3 pass, 2 partial**.

## First GREEN observation

The evaluator added a reusable limit/counterexample column and described conditions under which the preferred plan changes, but still collapsed the assumption into one ordinary claim row. The reminder was therefore replaced by a positive `ASSUMPTION AUDIT` output shape rather than another prohibition.

## Refactor GREEN observation

The evaluator returned:

- the traceable 4% mean-cost comparison and its 12-window evidence boundary;
- the observed counterexamples: five non-winning windows and a 15-ten-thousand-yuan worse tail result;
- an explicit reversal condition tied to demand paths and risk preference;
- a separate assumption table with necessity, `unverified` basis, affected relation/bias, cross-month correlation check and scenario recalibration action;
- deletion of unsupported significance and universal-applicability language.

Verdict: **5/5 pass**. The update preserves already-good behavior and adds only the missing structural fields.
